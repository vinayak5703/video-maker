services:
  - type: web
    name: video-saas-backend
    env: node
    plan: free
    buildCommand: npm run backend:install
    startCommand: npm run backend:start
    envVars:
      - key: PORT
        value: "5000"
